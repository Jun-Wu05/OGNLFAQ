---
name: ognl-painless
description: 帮写 OGNL / Painless / 字段引用表达式。用户给「样例日志 + 意图 + 期望效果」，skill 生成 1–2 个可直接粘贴进「添加字段」的表达式，并按诊断式对话收敛到可用结果。
version: 0.1.0
---

# OGNL / Painless 表达式生成 Skill

> 本 SKILL.md 是 skill 的永久加载正文。当前版本 `0.1.0` 开发中：骨架与版本治理（#9）、薄契约（#10）、FAQ 例子 KB（#11）、输入三元组（#12）已落地；路由指令、诊断循环、白名单、边界防御、收藏、回归集、盲区记账由后续切片（#13–#25）填充，发布收尾在 #26。

## 部署形态（SSOT：PRD「实现决策 › skill 形态：WisCode 会话模式」+ `docs/adr/0001`）

- **仓库源目录**：`skills/ognl-painless/`（本目录，随仓库 git 管理，是 skill 的版本治理 SSOT）。
- **全局部署目录**：`~/.wiscode/skills/ognl-painless/`（WisCode 全局 skill 加载位置）。
- **部署方式**：把仓库源目录的内容软链或拷贝到全局部署目录。改 prompt / 加案例 = 改仓库源目录文件后重新部署，无需起独立服务。
- **运行时复用**：skill 复用 WisCode 运行时 provider、文件系统与主对话模型；生成阶段由主对话模型完成，skill 只约束输入格式、注入 KB、规范输出、定义诊断循环。

## 输入三元组约束（Issue #12 · SSOT：PRD「实现决策 › 输入三元组约束」）

skill 入口要求用户给齐三元组：`样例日志` + `意图` + `期望效果`。

- **样例日志**：一段真实/拟造日志原文，作为表达式生成与验证的输入。
- **意图**：自然语言描述「想对样例日志做什么」。
- **期望效果**：须写成「应新增字段 xxx，值应为 yyy」的可逐字判等形式。`期望效果` 是后续判等与诊断收敛的锚点，缺了它工具无法判「跑通」。

**校验规则：**

- 三元组缺 `样例日志` / `意图` / `期望效果` 任一 → **引导用户补齐，不进入生成**。给出缺哪一项、怎么补的提示，不要凭空编造输入。
- `期望效果` 非「应新增字段 xxx，值应为 yyy」形式 → **提示用户改写**到该形式再生成。例如「我想提取邮箱」不是合法 `期望效果`，应改写为「应新增字段 email，值应为提取到的邮箱地址」。
- 三元组齐备且 `期望效果` 合法后，才进入薄契约生成阶段。

## 薄契约注入：三种语法边界（Issue #10 · SSOT：PRD「实现决策 › 薄契约注入」+ `docs/eval/README.md`「跑批注入上下文 · 换皮 17 条 · 纯语法薄契约」）

> 本段是跨题通用的语法边界契约，**逐字同 `docs/eval/README.md`「跑批注入上下文 · 换皮 17 条 · 纯语法薄契约」**，不含任何 FAQ 写法示例（示例归 KB，见 `kb/faq-examples.md`）。

你在为一套解析规则「添加字段」生成表达式。可用三种语法：
- <% … %>  OGNL 表达式，返回任意 Java 对象，前后不能拼接字符串
- {% … %}  Painless 表达式，类 Java/Groovy，前后不能拼接字符串
- %{ … }   最基本字段引用，前后可拼接字符串
针对下面的样例日志与意图，输出 1–2 个可直接粘贴的表达式（`<% %>` / `{% %}` / `%{ }` 三种之一），按出现顺序的第 1 个表达式块即为你最推荐的首选，它前面可写自然语言说明（说明不计入首选）。

**契约要点（与上面薄契约同源，供执行时对照）：**

- 三种语法定义与「前后不能/可拼接字符串」约束是边界正确性的硬要求，混淆边界符贴进平台必报错。
- 「输出 1–2 个可直接粘贴的表达式」是交付形态要求——裸串（缺边界符）跑不通，等同交付失败。
- 「按出现顺序的第 1 个表达式块为首选」是判分锚点（首选制逐字判等）：模型常先写一段说明再给式子，前言说明不计入首选，取整条输出里第 1 个 `<% %>` / `{% %>` / `%{ }` 表达式块。
- 默认按 OGNL / Painless 任选最合适的语言，不强制用某一种，贴合现场本脑对该字段的能力支持。

## FAQ 例子 KB（Issue #11 · SSOT：PRD「实现决策 › FAQ 例子 KB 注入」+ `docs/reference/OGNL常用写法FAQ.md`）

KB 与薄契约分离：薄契约是上面那段跨题通用语法边界，KB 是按写法分类的例子。KB 正文落在 `kb/faq-examples.md`，弱档 model 按需读 KB 例子，强档 model 直出不读（路由见下文，由 #13 落地）。

KB 条目结构：每条含「写法名 + 原句示例 + 适用语法」。当前覆盖第二条件跑批中拉过线的高频写法：大小写转换、三元取值去空白、split 取位、`distinct` 去重、域名还原、正则抽邮箱、递归扁平化。

## 待落地段（占位，由后续切片填充）

- **按档路由指令 + 弱档 model 清单** — Issue #13（LLM 自读身份，弱档读 KB / 强档直出）
- **L1/L2/L3 回归 + 陷阱补强** — Issue #14 / #15 / #16
- **诊断式修正循环** — Issue #17（贴回报错/输出值 → 给修正方向，不自动重试）
- **白名单契约侧兜底** — Issue #18（静态类仅限白名单四类）
- **空值/边界防御指引** — Issue #19
- **收藏落盘** — Issue #20（跑通表达式存 `docs/favorites/`，收藏 ≠ Q&A）
- **L3 重点回归集 / edge 白名单回归集** — Issue #21 / #22
- **软硬分离盲区记账** — Issue #23（标量类型盲区标注 + 盲区值错降级口径）

> 测试 SSOT：`docs/eval/README.md`（判定规则预注册）+ `docs/eval/results.md`（两轮跑批实跑结果）。判分锚点 = 现场平台实跑，首选制逐字判等。
