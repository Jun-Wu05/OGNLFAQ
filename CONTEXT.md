# CONTEXT

> 本文件记录项目的领域语言、核心概念与术语约定。
> 工程 skills（to-prd、diagnose、tdd、improve-codebase-architecture 等）会读取它来对齐用词。
> 按需填写，未确定的部分可以留空——`/grill-with-docs` 会在决策成型时补充。

## 项目一句话

帮写 OGNL / Painless 特殊表达式的小工具：用户给出样例日志 + 自然语言意图 + 期望效果，工具生成 1–2 种可操作的表达式，并通过诊断式对话收敛到可用结果。

## 核心概念 / 术语表

| 术语 | 定义 | 避免使用的同义词 |
| ---- | ---- | ---------------- |
| OGNL 表达式 | 解析规则「添加字段」中以 `<%` 开头、`%>` 结尾的表达式，返回任意 Java 对象；前后不能拼接字符串 | OGNL 语句、ognl 语法 |
| Painless 表达式 | 以 `{%` 开头、`%}` 结尾的表达式，类 Java/Groovy 脚本，返回任意 Java 对象；前后不能拼接字符串 | painless 脚本、painless 语句 |
| 字段引用 | 以 `%{` 开头、`}` 结尾的最基本字段引用，前后可拼接任意字符串 | 占位符 |
| 样例日志 | 用户贴入的一段真实/拟造日志原文，作为表达式生成与验证的输入 | 原始日志、raw log |
| 意图 | 用自然语言描述「想对样例日志做什么」 | 需求 |
| 期望效果 | 用「应新增字段 xxx，值应为 yyy」的写法描述处理后的字段值，须可逐字判等 | 输出、结果 |
| 场景 | 从 Q&A / 资料提炼的一条可验证用例，含样例日志、意图、期望效果 | 用例、case |
| 可逐字判等 | 期望效果写成「应新增字段 xxx，值应为 yyy」，生成结果与之逐字比对即可判分 | 判等标准 |
| 诊断式对话 | 表达式在现场没跑通时，用户把本脑报错原文或实际输出值贴回，工具据此给出 1–2 个修正方向的新表达式，由用户再拿去现场试；工具不自动重试 | 调试、纠错循环 |
| 收藏 | 经现场验证跑通的表达式条目，自动存入 `docs/favorites/`；不等于 Q&A | 收藏夹、favorites |
| 添加字段 | 现场本脑解析规则里使用表达式的功能入口 | —— |

## 边界与非目标

- 不做表达式自动重试：工具不替用户猜测报错，必须由用户贴回真实反馈再迭代。
- 不自动写入 Q&A 文档：跑通只进收藏；Q&A 追加交给后续带标准的 skill 完成。
- 不在本工具内做版本能力数据库：3.5 与高版本的能力差异由用户在对话中说明，工具不维护版本矩阵。

## 关键约束

- 生成结果必须可在现场本脑「添加字段」中直接粘贴使用，语法边界（`<% %>` / `{% %}` / `%{ }`）不能混淆。
- 样例日志可能含现场敏感信息，工具侧不做强制脱敏，但提示用户自行判断。
